/*operadorternario */

/*El operardor ternario sirve para sustituir una estructura de flujo basica 
como seria la de if 
depues de evaluar aplica una condicion u otra
*/
const mensaje = edad > 18 ? `eres mayor de edad` : `eres menor de edad`;
document.write(`<p>${mensaje}</p>`);

